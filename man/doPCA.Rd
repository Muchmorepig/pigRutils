% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doPCA.R
\name{doPCA}
\alias{doPCA}
\title{This function performs PCA on input data and extracts specified principal components
while preserving group information.}
\usage{
doPCA(
  indata,
  coldata,
  intgroup = "groups",
  ntop = ifelse(0.1 * nrow(indata) > 2000, 0.1 * nrow(indata), 2000),
  pcsToUse = 1:3
)
}
\arguments{
\item{indata}{A numeric matrix or data frame containing the input data
(typically normalized gene expression data)}

\item{coldata}{A data frame containing sample metadata/annotations}

\item{intgroup}{A character string specifying the column in coldata to use for grouping
(default is "groups")}

\item{ntop}{Number of top variable features to use for PCA calculation.
Defaults to the minimum of 2000 or 10\% of total features.}

\item{pcsToUse}{A numeric vector specifying which principal components to extract
(default is first 3 components)}
}
\value{
A data frame containing:
\itemize{
\item Principal component coordinates for selected components
\item Group information
\item Sample names
With an attribute "percentVar" showing the percentage of variance explained
by the selected principal components
}
}
\description{
This function performs PCA on input data and extracts specified principal components
while preserving group information.
}
\examples{
# Assuming you have expression data and sample metadata
expr_data <- matrix(rnorm(1000), ncol=10)
sample_info <- data.frame(groups = c(rep("A", 5), rep("B", 5)))
pca_result <- doPCA(expr_data, sample_info)

}
